import "../styles/dashboard.css"; // 🔹 Importação do CSS

const Dashboard = () => {
    return (
        <div className="dashboard-container">
            <h2 className="dashboard-title">SisPromo</h2>
        </div>
    );
};

export default Dashboard;
